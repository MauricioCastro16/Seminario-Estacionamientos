@model IEnumerable<estacionamientos.Models.TarifaServicio>

@{
    ViewData["Title"] = "Tarifas Vigentes";
    var playa = ViewBag.Playa as estacionamientos.Models.PlayaEstacionamiento;
}

<div class="card shadow-sm mt-4">
    <div class="card-body">
        <h5 class="card-title">
            <i class="bi bi-geo-alt-fill text-primary"></i>
            @playa?.PlyNom
        </h5>

        <h6 class="mt-3">Tarifas vigentes:</h6>
        @if (!Model.Any())
        {
            <div class="alert alert-warning mt-2">
                Esta playa aún no tiene tarifas vigentes cargadas.
            </div>
        }
        else
        {
            <table class="table table-hover mt-2">
                <thead>
                    <tr>
                        <th>Servicio</th>
                        <th>Clasificación Vehículo</th>
                        <th>Monto</th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var tarifa in Model)
                {
                    <tr>
                        <td>@tarifa.ServicioProveido.Servicio.SerNom</td>
                        <td>@tarifa.ClasificacionVehiculo.ClasVehTipo</td>
                        <td>$@tarifa.TasMonto</td>                    </tr>
                }
                </tbody>
            </table>
        }

        <a asp-controller="PlayaEstacionamiento" asp-action="DetailsPlayero" asp-route-id="@playa?.PlyID"
           class="btn btn-outline-secondary mt-3">Volver
        </a>
    </div>
</div>
