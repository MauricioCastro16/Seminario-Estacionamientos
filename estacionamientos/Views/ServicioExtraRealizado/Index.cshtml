@model IEnumerable<estacionamientos.Models.ServicioExtraRealizado>

@{
    ViewData["Title"] = "Servicios Extra Realizados";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2 class="mb-0">Servicios Extra</h2>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> Registrar nuevo servicio
    </a>
</div>


@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

<p class="text-muted">
    Playa: <strong>@ViewBag.PlayaNombre</strong><br />
</p>

<table class="table table-striped align-middle">
    <thead>
        <tr>
            <th>Servicio</th>
            <th>Patente</th>
            <th>Estado</th>
            <th>Comentario</th>
        </tr>
    </thead>
    <tbody>
        @if (!Model.Any())
        {
            <tr>
                <td colspan="5" class="text-center text-muted">No hay servicios registrados en este turno.</td>
            </tr>
        }
        else
        {
            foreach (var s in Model)
            {
                <tr>
                    <td>@s.ServicioProveido?.Servicio?.SerNom</td>
                    <td>@s.VehPtnt</td>
                    <td>
                        @if (string.IsNullOrEmpty(s.ServExEstado))
                        {
                            <span class="badge bg-secondary">Sin estado</span>
                        }
                        else if (s.ServExEstado == "Pendiente")
                        {
                            <span class="badge bg-warning text-dark">Pendiente</span>
                        }
                        else if (s.ServExEstado == "Completado")
                        {
                            <span class="badge bg-success">Completado</span>
                        }
                        else if (s.ServExEstado == "Cancelado")
                        {
                            <span class="badge bg-danger">Cancelado</span>
                        }
                        else
                        {
                            <span class="badge bg-secondary">@s.ServExEstado</span>
                        }
                    </td>
                    <td>@s.ServExComp</td>
                </tr>
            }
        }
        </tbody>

</table>
